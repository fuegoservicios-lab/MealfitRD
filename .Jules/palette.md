## 2024-04-16 - Semantic Mobile Menus
**Learning:** Icon-only navigation toggles in the header component structure require explicit `aria-label`, `aria-expanded`, and `aria-controls` properties to be accessible to screen reader users, especially given their interactive state.
**Action:** Always include semantic ARIA attributes when implementing interactive elements that use only icons for their visual representation.
