{
  "_doc": "[P1-PLAN-LOTE-108] SSOT de las actualizaciones en vivo de la app nativa. enabled=false deja de ofrecer paquetes; reset=true devuelve a todos al paquete del binario. minNativeBuild = build number MINIMO del binario capaz de correr esta web: SUBELO (al numero del proximo build de Codemagic) cada vez que cambie nativeDeps, y lanza ese build. lote108.test.js falla si nativeDeps no coincide con package.json.",
  "enabled": true,
  "reset": false,
  "minNativeBuild": 13,
  "_nota_lote_152": "@capacitor/android entra SIN subir minNativeBuild, por la misma razon que local-notifications en el 133: es una PLATAFORMA nueva, no una capacidad nueva del binario de iOS. Subir el umbral habria dejado sin OTA a los iPhone instalados por un cambio que no les afecta. El primer APK nace con versionCode 100 (android/app/build.gradle), por encima del umbral, asi que Android si recibe OTA desde el dia uno.",
  "_nota_lote_160": "MfGoogleId (Credential Manager) entra SIN subir minNativeBuild, por la MISMA razon que local-notifications en el 133 y @capacitor/android en el 152: la web pregunta nativePluginAvailable('MfGoogleId') antes de usarlo, y un binario sin el plugin cae solo al camino de siempre (en Android eso es esconder el boton, que es lo que ya hacia). Subir el umbral dejaria sin OTA a los iPhone y al APK 101 instalados por una capacidad que no les afecta. Las tres dependencias nuevas son de GRADLE, no de npm: no tocan nativeDeps y por eso lote108.test.js sigue casando con package.json.",
  "_nota_lote_163": "@capacitor/app (el gesto atras de Android) entra SIN subir minNativeBuild, por la MISMA razon que el 133, el 152 y el 160: src/native/botonAtras.js pregunta nativePluginAvailable('App') antes de importarlo, y en un binario sin el plugin no registra nada (queda como hoy). Subir el umbral dejaria sin OTA a los iPhone y a los APK 100-102 instalados por una capacidad que solo trae el APK 103. El deploy del 22-sep lo paro lote108.test.js porque el lote 163 anadio la dependencia sin anotarla aqui.",
  "_nota_lote_170": "SystemBars insetsHandling 'disable' (capacitor.config.ts, APK 104) entra SIN subir minNativeBuild: es config del binario, no una dependencia (nativeDeps no cambia), y la web no depende de ella: con el APK 103 el teclado de Android sigue como hoy y con el 104 la WebView vuelve a encoger con adjustResize. Subir el umbral dejaria sin OTA a los APK 100-103 y a los iPhone por un cambio que solo trae el binario nuevo.",
  "nativeDeps": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/app": "^8.1.1",
    "@capacitor/camera": "^8.2.3",
    "@capacitor/core": "^8.5.0",
    "@capacitor/filesystem": "^8.1.3",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/ios": "^8.5.0",
    "@capacitor/local-notifications": "^8.3.1",
    "@capacitor/push-notifications": "^8.1.2",
    "@capacitor/share": "^8.0.2",
    "@capawesome/capacitor-live-update": "^8.4.2"
  },
  "_nota_lote_133": "@capacitor/local-notifications entra SIN subir minNativeBuild, a proposito: el JS pregunta nativePluginAvailable('LocalNotifications') antes de importarlo y en un binario sin el plugin muestra 'Actualiza la app...'. Subirlo habria dejado sin OTA a los binarios instalados hasta el build nuevo.",
  "_nota_lote_300": "@capacitor/share y @capacitor/filesystem (compartir la IMAGEN del dia) entran SIN subir minNativeBuild, por la MISMA razon que el 133, 152, 160 y 163: src/utils/compartirDia.js pregunta nativePluginAvailable('Share') y ('Filesystem') antes de usarlos (compartirNativo.lote300.test.js), y un binario sin ellos cae a la Web Share de siempre. La push de iOS (aps-environment, AppDelegate) es config del binario, no dependencia: llega con el TestFlight nuevo sin cerrar la OTA a los instalados."
}
